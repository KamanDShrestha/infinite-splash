import { Outlet } from 'react-router-dom';

// This component acts as layout component for providing different UI based on URL
const AppLayout = () => {
  return <Outlet />;
};

export default AppLayout;
